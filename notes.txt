Change working directory
--------------------------

ALLEGRO_PATH* p = al_get_standard_path(ALLEGRO_EXENAME_PATH);
al_change_directory(al_path_cstr(p) , ALLEGRO_NATIVE_PATH_SEP);
al_destroy_path(p);
p = 0;


Transfer mode
----------------
HUD text is 'captured'

Choose 'color' side - countdown 96

MODE_COUNTDOWN

- keyboard: left / right for side. Action to change mode


MODE_TRANSFER_GAME

- keyboard - up / down for current token. Action to place token

Transfer elements:

Lines
	Full line
	Half line
	3/4 Line
	Quarter line

	One into two - three slots
	Two into one - three slots
	Two into two - single middle

	Repeater

	Reverse color

12 rows
1 status header
Color block - right
Color block - left
Side bar, left / right - color
Join bar for split circuits - color
Launch block - black

Positions

Left Bar start
SideBar Width
Row square width ( draw center )
Row square height - 12 pixels
Status header - double row height
Token waiting position - lines up with first row



void trn_drawToken (int direction, int color, v2Vec position)